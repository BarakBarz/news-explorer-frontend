import React from 'react';

const Register = () => {
  return (
    <form name='register' className='register-form'>
      <input type='text' />
    </form>
  );
};

export default Register;
